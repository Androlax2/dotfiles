# amdgpu loads in the real root (the initramfs has no kms hook). X must not start on the
# firmware framebuffer and lose it when the driver takes over, so wait for amdgpu-load.service.
# After= only orders: if that unit fails, the greeter still starts.
[Unit]
After=amdgpu-load.service
