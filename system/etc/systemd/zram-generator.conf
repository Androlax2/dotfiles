# Half of RAM compressed with zstd is far more than this machine pages out; the
# /swapfile in fstab stays as low-priority overflow. Tuning in sysctl.d/99-zram.conf.
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
