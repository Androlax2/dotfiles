# zram is fast, so let the kernel swap cold anonymous pages to it eagerly rather
# than dropping page cache (Arch wiki, "zram", recommended values for zram-only swap).
vm.swappiness = 180
vm.watermark_boost_factor = 0
vm.watermark_scale_factor = 125
vm.page-cluster = 0
