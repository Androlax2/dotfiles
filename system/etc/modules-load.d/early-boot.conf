# Drivers the login path needs, loaded before udev starts its coldplug. Loading amdgpu holds
# the kernel's module loader for ~2 s (measured with initcall_debug, Sep 2026), and every
# module udev requests in that window waits: /boot (vfat) could not mount and NetworkManager
# had no interfaces, which delayed basic.target and sddm. These are small and load in ms.
vfat
r8169
rtw89_8922ae
# USB Ethernet in the monitor dock; NetworkManager waits for it before sddm can start.
r8152
